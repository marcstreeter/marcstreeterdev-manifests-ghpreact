pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,jsx,ts,tsx,json,css}"
      run: npx biome check {staged_files}
      stage_fixed: true
    tests:
      run: npm test || true
      skip_output:
        - meta
        - success 