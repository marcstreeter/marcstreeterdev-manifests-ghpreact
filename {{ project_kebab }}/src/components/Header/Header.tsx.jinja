import { AppBar, Toolbar, Typography, IconButton, Box } from '@mui/material';
import { Menu as MenuIcon } from '@mui/icons-material';

export const Header = () => {
  return (
    <AppBar position="static">
      <Toolbar>
        <IconButton
          size="large"
          edge="start"
          color="inherit"
          aria-label="menu"
          sx={% raw %}{{ mr: 2 }}{% endraw %}
        >
          <MenuIcon />
        </IconButton>
        <Typography variant="h6" component="div" sx={% raw %}{{ flexGrow: 1 }}{% endraw %}>
          {{ project_name }}
        </Typography>
        <Box sx={% raw %}{{ display: { xs: 'none', md: 'flex' } }}{% endraw %}>
          {/* Additional header actions can be added here */}
        </Box>
      </Toolbar>
    </AppBar>
  );
};